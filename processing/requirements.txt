# ==============================================================================
# AeroLearn Local AI Core API Engine — Python Dependency Stack
# ==============================================================================

# --- WEB FRAMEWORK & ROUTING LAYER ---
# FastAPI provides the highly performant asynchronous ASGI routing matrix
fastapi>=0.95.2
# Uvicorn standard serves the FastAPI application with high-throughput performance
uvicorn[standard]>=0.18.3

# --- DISTRIBUTED BACKGROUND QUEUE LAYER ---
# Celery orchestrates out-of-band execution of heavy local and remote AI pipeline tasks
celery>=5.2.7
# Redis acts as the high-speed in-memory message broker and task result backend
redis>=4.5.4

# --- LOCAL AI & SPEECH DECODER SYSTEM ---
# Faster-Whisper is a highly optimized CTranslate2-based implementation of OpenAI's Whisper model
faster-whisper>=0.10.0
# pytesseract is the Tier 2 OCR engine (between pdfplumber native text and GPT-4o Vision).
# It handles most scanned-PDF images efficiently. GPT-4o Vision is only called when
# pytesseract's per-word confidence score falls below the 60% threshold.
pytesseract>=0.3.10

# --- REMOTE AI & AVATAR GENERATION APIS ---
# AssemblyAI client connects to neural transcription APIs for auto-chapter segmentation
assemblyai>=0.5.1
# OpenAI integration for GPT-4 features
openai>=1.12.0
# Noise reduction for blind mode voice capture
noisereduce>=3.0.0
numpy>=1.22.0
scipy>=1.10.0

# --- MULTI-FORMAT DOCUMENT & IMAGE PARSERS ---
# PDFPlumber parses native vector PDFs and renders visual pages for local vision-based OCR fallback
pdfplumber>=0.9.1
# Pillow manages raw image transformation and formats images for neural Vision-Language-Models
Pillow>=9.2.0

# --- PDF GENERATION & LATEX RENDERER ---
fpdf2>=2.7.0
matplotlib>=3.5.0

# --- MEDIA INGESTION & HTTP CORE CLIENTS ---
# yt-dlp fetches and extracts audio streams from remote YouTube lectures
yt-dlp>=2022.10.14
# Requests client manages synchronous HTTP payloads for polling remote avatar rendering APIs (HeyGen)
requests>=2.28.2

# --- FASTAPI FORM DATA SUPPORT ---
python-multipart>=0.0.7